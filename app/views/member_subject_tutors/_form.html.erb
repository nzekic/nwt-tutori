<%= form_for(@member_subject_tutor) do |f| %>
  <% if @member_subject_tutor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member_subject_tutor.errors.count, "error") %> prohibited this member_subject_tutor from being saved:</h2>

      <ul>
      <% @member_subject_tutor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tutor_subject_id %><br>
    <%= f.number_field :tutor_subject_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br>
    <%= f.number_field :member_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
