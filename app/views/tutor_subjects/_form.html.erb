<%= form_for(@tutor_subject) do |f| %>
  <% if @tutor_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tutor_subject.errors.count, "error") %> prohibited this tutor_subject from being saved:</h2>

      <ul>
      <% @tutor_subject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject_id %><br>
    <%= f.number_field :subject_id %>
  </div>
  <div class="field">
    <%= f.label :tutor_id %><br>
    <%= f.number_field :tutor_id %>
  </div>
  <div class="field">
    <%= f.label :rateperhour %><br>
    <%= f.text_field :rateperhour %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
